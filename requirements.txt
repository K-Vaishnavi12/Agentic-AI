# Offline Corporate Intelligence Platform
# Python 3.10+

# ── LLM & orchestration ───────────────────────────────────────────────
langgraph>=0.2.0
langchain>=0.3.0
langchain-community>=0.3.0
langchain-ollama>=0.2.0
ollama>=0.3.0

# ── Vector memory ─────────────────────────────────────────────────────
chromadb>=0.4.0
langchain-chroma>=0.1.0
sentence-transformers>=2.2.0

# ── Scheduling & async ────────────────────────────────────────────────
apscheduler>=3.10.0
aiohttp>=3.9.0

# ── Data & config ─────────────────────────────────────────────────────
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# ── API ────────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.6

# ── Utilities ─────────────────────────────────────────────────────────
httpx>=0.27.0
tenacity>=8.2.0

# ── ML stack (forecasting / anomaly / NLP) ────────────────────────────
numpy>=1.26
pandas>=2.0
scikit-learn>=1.4
scipy>=1.11
joblib>=1.3
statsmodels>=0.14          # ARIMA, classical time-series
torch>=2.0                 # LSTM, autoencoder, transformer fine-tune
matplotlib>=3.8            # plots
seaborn>=0.13              # nicer plots / confusion matrix
transformers>=4.40         # DistilBERT / FinBERT
datasets>=2.18             # HF datasets (Financial PhraseBank)
accelerate>=0.30           # transformer training helper
evaluate>=0.4              # ROUGE, BLEU
rouge-score>=0.1.2         # ROUGE for LLM eval

# ── Live market data (Sensex, Nifty, stocks; one-time online) ─────────
yfinance>=0.2.0

# ── Dev / tests ───────────────────────────────────────────────────────
pytest>=8.0
